# Task 03A-11：Google Sheet 第一批 CRM 欄位新增完成紀錄

## 一、任務目的

本階段目的，是將 Task 03A 已定義的 CRM 名單階段與安全控制欄位，先以低風險方式新增到正式 Google Sheet 的 `leads_潛在會員名單` 分頁。

本階段不修改既有欄位、不刪除既有資料、不調整 Apps Script、不修改前端程式。

---

## 二、操作前確認

| 項目 | 結果 |
|---|---|
| 正式 Google Sheet | `元馨醫管家_平台主資料庫_v2` |
| 操作分頁 | `leads_潛在會員名單` |
| 是否已建立備份 | 是 |
| 是否先保存欄位截圖 | 是 |
| 是否修改既有欄位名稱 | 否 |
| 是否刪除既有欄位 | 否 |
| 是否修改程式碼 | 否 |
| 是否部署 | 否 |

---

## 三、備份紀錄

本次操作前已建立 Google Sheet 備份。

備份用途：

- 保留操作前資料表狀態
- 若新增欄位或驗證規則出現問題，可回查原始欄位與資料
- 作為正式 Sheet 結構調整前的保護點

---

## 四、本次新增欄位範圍

本次只在 `leads_潛在會員名單` 分頁既有最後欄位 `BA：內部備註` 右側新增欄位。

新增範圍：

```text
BB 到 BX
```

新增欄位如下：

| 欄位 | 欄位名稱 |
|---|---|
| BB | 原名單狀態 |
| BC | CRM階段 |
| BD | CRM階段轉換方式 |
| BE | CRM階段轉換時間 |
| BF | CRM階段轉換人 |
| BG | CRM階段需人工確認 |
| BH | CRM階段人工確認備註 |
| BI | CRM階段更新時間 |
| BJ | CRM階段更新人 |
| BK | CRM階段備註 |
| BL | 名單有效性 |
| BM | 無效原因 |
| BN | 是否測試資料 |
| BO | 是否重複資料 |
| BP | 合併至名單ID |
| BQ | 是否允許LINE訊息 |
| BR | 是否允許電話聯繫 |
| BS | 是否允許行銷訊息 |
| BT | 是否停止聯繫 |
| BU | 停止聯繫原因 |
| BV | 停止聯繫時間 |
| BW | 退訂來源 |
| BX | 停止聯繫操作者 |

---

## 五、CRM 階段下拉選單

`BC：CRM階段` 已設定下拉選單。

選項如下：

```text
01 新名單
02 待首次聯繫
03 已聯繫
04 培育中
05 已預約諮詢
06 方案評估中
07 已簽約（未付款）
08 已成交（付款完成）
09 暫緩
10 無意願
11 停止聯繫／退訂
```

已確認下拉選單共 11 個選項，內容正確。

---

## 六、安全欄位下拉選單

本階段已設定第一批安全欄位下拉選單。

### 1. CRM階段需人工確認

欄位：

```text
BG：CRM階段需人工確認
```

選項：

```text
是
否
```

### 2. 名單有效性

欄位：

```text
BL：名單有效性
```

選項：

```text
有效
無效
待確認
測試資料
重複資料
資料錯誤
無法聯繫
```

### 3. 是／否欄位

以下欄位已設定為 `是／否` 下拉選單：

| 欄位 | 欄位名稱 |
|---|---|
| BN | 是否測試資料 |
| BO | 是否重複資料 |
| BQ | 是否允許LINE訊息 |
| BR | 是否允許電話聯繫 |
| BS | 是否允許行銷訊息 |
| BT | 是否停止聯繫 |

選項：

```text
是
否
```

---

## 七、小量資料填入紀錄

本階段僅先填入第 2 到第 7 列，作為小量安全測試。

填入範圍：

```text
第 2 列到第 7 列
```

### 1. 原名單狀態

`BB2:BB7` 已填入與既有 `V：名單狀態` 相同的值。

因 `V：名單狀態` 為下拉選單，無法直接複製，因此採手動 key 入相同狀態。

目前填入：

```text
新名單
```

### 2. CRM階段

`BC2:BC7` 已填入：

```text
01 新名單
```

### 3. CRM階段需人工確認

`BG2:BG7` 已填入：

```text
是
```

### 4. 名單有效性

`BL2:BL7` 已填入：

```text
有效
```

### 5. 測試與重複資料

`BN2:BN7` 已填入：

```text
否
```

`BO2:BO7` 已填入：

```text
否
```

### 6. 聯繫與行銷授權

`BQ2:BQ7` 已填入：

```text
是
```

`BR2:BR7` 已填入：

```text
是
```

`BS2:BS7` 已填入：

```text
是
```

### 7. 停止聯繫

`BT2:BT7` 已填入：

```text
否
```

---

## 八、驗收結果

### 1. 欄位位置

已確認新增欄位位於 `BA：內部備註` 右側，範圍為 `BB:BX`。

Google Sheet 畫面因欄位較多，會分段顯示欄位，但欄位位置正確。

### 2. CRM階段下拉選單

已確認 `CRM階段` 下拉選單 11 個選項正確。

### 3. 安全欄位下拉選單

已確認第一批安全欄位下拉選單已設定。

### 4. 小量資料

已確認第 2 到第 7 列已填入第一批預設值。

### 5. 顧問業務大廳

已使用以下 LIFF 入口測試顧問業務大廳：

```text
https://liff.line.me/2009597152-9nSswjnk?portal=consultant
```

確認結果：

- 可正常進入顧問業務大廳
- 可看到顧問資料
- 可看到推薦名單
- 可看到顧問資源中心

### 6. 推薦事件寫入

已確認從「我的健康報告」入口進入時，推薦事件會寫入 `referral_events_推薦事件紀錄` 分頁底部。

確認結果：

- 新事件寫入最底部
- 未覆蓋既有資料

---

## 九、已知觀察

目前測試發現：

- 從「我的健康報告」入口會寫入推薦事件。
- 從官方 LINE 入口或會員首頁其他宮格選單進入時，尚未確認會寫入推薦事件。
- 直接使用 Google Drive 共享資料夾連結進入顧問資料夾，不會寫入推薦事件。

上述屬於後續「入口事件追蹤補強」範圍，不納入本次 Google Sheet 欄位新增任務。

---

## 十、顧問大廳入口策略

顧問業務大廳不建議放在官方 LINE 公開圖文選單，避免一般客戶看到顧問入口造成混淆。

後續建議採用：

```text
方案 A：LINE 關鍵字自動回覆按鈕
```

建議關鍵字：

```text
顧問大廳
顧問入口
業務大廳
```

按鈕連結：

```text
https://liff.line.me/2009597152-9nSswjnk?portal=consultant
```

---

## 十一、本階段未執行事項

本階段未執行：

- 未新增第二批 CRM 待辦與自動化欄位
- 未修改既有欄位名稱
- 未刪除既有欄位
- 未批次轉換全部歷史資料
- 未修改 Apps Script
- 未修改前端程式
- 未部署

---

## 十二、本階段結論

Task 03A-11 第一批 Google Sheet CRM 欄位新增已完成。

本階段完成內容：

- 已建立 Google Sheet 備份
- 已保存正式欄位截圖
- 已新增 `BB:BX` 第一批 CRM 與安全控制欄位
- 已設定 `CRM階段` 11 階段下拉選單
- 已設定第一批安全欄位下拉選單
- 已填入第 2 到第 7 列小量預設值
- 已完成顧問業務大廳與推薦事件基本驗收

建議下一步先暫停第二批欄位新增，待本次欄位結構穩定後，再進行 CRM 待辦、自動化流程與人工接手相關欄位。